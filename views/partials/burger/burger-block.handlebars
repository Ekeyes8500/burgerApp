<li>
	{{burger_name}}

	<button class="change-state" data-id="{{id}}" data-newstate="{{devoured}}">
		{{#if devoured}}REGURGITATE ME{{else}}EAT ME!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
